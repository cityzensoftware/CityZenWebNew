import { Box, Button, Typography } from "@mui/material";
import { FC } from "react";

const HomeContent: FC = () => {
  return (
    <Box>
      <Typography>HomeContentPage</Typography>
      <Button variant="contained">Go to grid</Button>
    </Box>
  );
};

export default HomeContent;
